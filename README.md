# memx-lab.github.io

Homepage repository template for the `memx-lab` GitHub organization.

## Purpose

This repository is the public-facing homepage, separate from the documentation portal.

It should answer:

- What is memx-lab?
- Why does it exist?
- Which projects belong to it?
- Where should users start?

## Suggested links from the homepage

- GitHub organization
- Documentation portal
- Core repositories
- Papers or technical notes
- Benchmarks and examples

## Local preview

Open `index.html` directly in a browser, or run:

```bash
python3 -m http.server 4173
```

Then visit:

- `http://localhost:4173`

## Deployment

Push this folder into a repository named:

- `memx-lab.github.io`

GitHub Pages will then publish it at:

- `https://memx-lab.github.io/`
